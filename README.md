# Knowledge_Distillation
This repo contains the script for Knowledge distillation of a SFT model.


distillation.py: main distillation training script


config.yaml: sets hyperparameters, file, model paths etc.


comaprison.py: streamlit app to fun inference with same parameters compare the student and teacher model.
